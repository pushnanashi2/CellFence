# OSS TS/JS 200-Repository Onboarding Run - 2026-07-18

This is a larger public-OSS CellFence onboarding run. It is not a precision
claim and it is not evidence that named upstream repositories contain defects.
All subjects used unreviewed `manifest.strategy: infer`, so the run measures
whether CellFence can clone fixed public commits, infer a non-destructive
starter manifest, execute deterministically, and preserve evidence for later
manual labeling.

No target repository dependencies were installed, no target package scripts were
executed, no upstream issues were opened, and target checkouts were discarded
after manifests and logs were preserved.

## Corpus

Corpus manifest: `docs/research/corpora/oss-ts-js-200-2026-07-18.json`

Selection:

- 200 public GitHub repositories;
- 100 primary-language TypeScript repositories and 100 primary-language
  JavaScript repositories;
- non-archived, non-fork repositories;
- repository `diskUsageKb < 50000`;
- TypeScript query used `stars:>=500`;
- JavaScript query used `stars:>=1000`;
- exact 40-hex default-branch commits were recorded before running checks;
- all subjects used `manifest.strategy: infer`.

The corpus was fixed before the run. Because these are unreviewed inferred
manifests, results are suitable for onboarding and tuning analysis only.

## Commands

Dry run:

```bash
npm run research:corpus -- \
  --corpus reports/corpus/oss-ts-js-200-2026-07-18.corpus.json \
  --out reports/corpus/oss-ts-js-200-2026-07-18.dry-run.json \
  --workdir tmp/corpus-precision-study-200 \
  --dry-run
```

Run:

```bash
npm run research:corpus -- \
  --corpus reports/corpus/oss-ts-js-200-2026-07-18.corpus.json \
  --out reports/corpus/oss-ts-js-200-2026-07-18.json \
  --workdir tmp/corpus-precision-study-200 \
  --clone-mode shallow \
  --discard-checkouts
```

Evidence bundle:

```bash
npm run research:bundle -- \
  --study-id oss-ts-js-200-2026-07-18 \
  --corpus reports/corpus/oss-ts-js-200-2026-07-18.corpus.json \
  --report reports/corpus/oss-ts-js-200-2026-07-18.json \
  --out-dir reports/corpus/oss-ts-js-200-2026-07-18-bundle \
  --force

npm run research:bundle -- \
  --validate \
  --bundle reports/corpus/oss-ts-js-200-2026-07-18-bundle
```

## Summary

| metric | value |
|---|---:|
| subjects | 200 |
| clone/checkout/manifest completed | 200 |
| failed before check | 0 |
| CellFence checks run | 200 |
| checks with exit 0 | 14 |
| checks with findings | 186 |
| configuration errors | 0 |
| tool errors | 0 |
| unparseable outputs | 0 |
| timeouts | 0 |
| CLI error findings | 27,817 |
| raw normalized audit findings, including warnings | 37,654 |
| deterministic labeling sample | 651 |
| harness dirty | false |

Subject duration distribution:

| percentile | duration |
|---|---:|
| p50 | 3.3s |
| p90 | 6.6s |
| p99 | 22.9s |

Findings by rule:

| rule | findings |
|---|---:|
| `CELLFENCE_PRIVATE_IMPORT` | 23,760 |
| `CELLFENCE_UNDECLARED_RESOURCE_ACCESS` | 1,722 |
| `CELLFENCE_UNRESOLVED_IMPORT` | 700 |
| `CELLFENCE_UNDECLARED_CONSUMER` | 668 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_IMPORT` | 407 |
| `CELLFENCE_UNRESOLVED_RESOURCE_ACCESS` | 284 |
| `CELLFENCE_UNOWNED_IMPORT_TARGET` | 138 |
| `CELLFENCE_PUBLIC_ENTRY_MISSING` | 95 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_REQUIRE` | 23 |
| `CELLFENCE_OWNERSHIP_OVERLAP` | 19 |
| `CELLFENCE_SYMLINK_TARGET_OUTSIDE_OWNERSHIP` | 1 |

Top subjects by finding count:

| subject | findings | notes |
|---|---:|---|
| `Gitlawb/openclaude` | 10,541 | dominated by private imports and dynamic imports |
| `umami-software/umami` | 2,211 | dominated by private imports |
| `typeorm/typeorm` | 1,675 | private imports plus unresolved resources |
| `honojs/hono` | 1,650 | resource-access heavy; also found the empty SQL query crash before the fix |
| `ajaxorg/ace` | 1,382 | private imports plus unresolved imports |
| `maotoumao/MusicFree` | 1,067 | private imports plus unowned import targets |
| `Yeachan-Heo/oh-my-codex` | 826 | private imports plus many warning-severity audit findings |
| `HeyPuter/puter` | 811 | overlap, consumer, private import, and resource findings |
| `rollup/rollup` | 499 | private imports plus unresolved imports |
| `nanocoai/nanoclaw` | 479 | dominated by private imports |

The 14 zero-finding subjects were:

`JCodesMore/ai-website-cloner-template`, `JedWatson/react-select`,
`SortableJS/Sortable`, `affaan-m/ECC`, `alvarotrigo/fullPage.js`,
`bmad-code-org/BMAD-METHOD`, `chenglou/pretext`,
`google-labs-code/design.md`, `h5bp/html5-boilerplate`,
`impress/impress.js`, `lerna/lerna`, `markedjs/marked`,
`react/create-react-app`, and `tailwindlabs/headlessui`.

## Bug Found

The first 200-subject run found a CellFence CLI crash on `honojs/hono`:
`Cannot read properties of undefined (reading 'kind')`. The trigger was a
zero-argument SQL-like `.query()` call. The SQL literal detector now accepts an
undefined first argument and the regression is covered by
`tests/resource-access-coverage.test.mjs`.

After the fix, the same corpus completed 200/200 subjects with no tool errors
or unparseable outputs.

## Interpretation

What this run shows:

- CellFence can run across 200 fixed public repositories without installing or
  executing target packages.
- Shallow clone and checkout-discard modes keep local disk usage practical for
  larger solo experiments.
- Real public repositories quickly expose implementation bugs that synthetic
  fixtures missed.
- The evidence bundle is sufficient to start deterministic manual labeling.

What this run does not show:

- It does not establish precision, recall, or false-positive rate.
- It does not prove that any named upstream repository is wrong.
- It does not validate inferred manifests as architecture contracts.
- It does not compare CellFence against Nx, dependency-cruiser, ESLint
  boundaries, CODEOWNERS, or package export maps.

## Labeling Backlog

Next work should use the 651-sample evidence bundle, not the full raw finding
set, and classify each sampled finding as `true_positive`, `false_positive`,
`needs_policy`, `invalid_setup`, or `out_of_scope`.

Priority slices:

- `CELLFENCE_PUBLIC_ENTRY_MISSING`: measure inference fallback rate and improve
  onboarding heuristics.
- `CELLFENCE_PRIVATE_IMPORT`: distinguish real boundary reaches from inferred
  cells that should be merged or translated from existing project boundaries.
- `CELLFENCE_UNDECLARED_RESOURCE_ACCESS`: separate true resource contract gaps
  from test fixtures, build scripts, and intentionally dynamic paths.
- `CELLFENCE_OWNERSHIP_OVERLAP`: manually inspect the 19 findings in
  `eyaltoledano/claude-task-master`, `HeyPuter/puter`, and
  `modelcontextprotocol/servers`.

Only after that pass should this corpus be used in any public precision number.

## Codex Pilot Labeling Pass

Label file:
`docs/research/labels/oss-ts-js-200-2026-07-18.codex-pilot.labels.jsonl`

This is an AI-assisted pilot labeling pass over the 651 deterministic sample
findings. It is a calibration artifact, not a public precision denominator. The
pass used a rule rubric plus representative source spot-checks for ambiguous
families. It did not fully review every target repository's intended
architecture.

Labeled bundle command:

```bash
npm run research:bundle -- \
  --study-id oss-ts-js-200-2026-07-18-codex-pilot-labels \
  --corpus reports/corpus/oss-ts-js-200-2026-07-18.corpus.json \
  --report reports/corpus/oss-ts-js-200-2026-07-18.json \
  --labels docs/research/labels/oss-ts-js-200-2026-07-18.codex-pilot.labels.jsonl \
  --out-dir reports/corpus/oss-ts-js-200-2026-07-18-codex-pilot-labels-bundle \
  --force

npm run research:bundle -- \
  --validate \
  --bundle reports/corpus/oss-ts-js-200-2026-07-18-codex-pilot-labels-bundle
```

Pilot labels:

| label | count |
|---|---:|
| `needs_policy` | 199 |
| `out_of_scope` | 192 |
| `invalid_setup` | 176 |
| `true_positive` | 51 |
| `needs_review` | 33 |

Confidence:

| confidence | count |
|---|---:|
| high | 166 |
| medium | 452 |
| low | 33 |

Rule-by-label matrix:

| rule | pilot labels |
|---|---|
| `CELLFENCE_OWNERSHIP_OVERLAP` | invalid_setup 19 |
| `CELLFENCE_PRIVATE_IMPORT` | needs_policy 134; out_of_scope 20 |
| `CELLFENCE_PUBLIC_ENTRY_MISSING` | invalid_setup 95 |
| `CELLFENCE_SYMLINK_TARGET_OUTSIDE_OWNERSHIP` | true_positive 1 |
| `CELLFENCE_UNDECLARED_CONSUMER` | needs_policy 47; out_of_scope 7 |
| `CELLFENCE_UNDECLARED_RESOURCE_ACCESS` | needs_policy 18; out_of_scope 35 |
| `CELLFENCE_UNOWNED_IMPORT_TARGET` | invalid_setup 51 |
| `CELLFENCE_UNRESOLVED_IMPORT` | needs_review 28; out_of_scope 15; invalid_setup 11 |
| `CELLFENCE_UNRESOLVED_RESOURCE_ACCESS` | out_of_scope 52; true_positive 39; needs_review 5 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_IMPORT` | out_of_scope 49; true_positive 2 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_REQUIRE` | out_of_scope 14; true_positive 9 |

Pilot interpretation:

- Most sample findings are not ready to become precision evidence because the
  manifests were inferred, unreviewed, and often intentionally empty of resource
  contracts.
- `PUBLIC_ENTRY_MISSING`, broad/nested `OWNERSHIP_OVERLAP`, and asset/style
  `UNOWNED_IMPORT_TARGET` findings are primarily onboarding or inference setup
  problems.
- Many resource and private-import findings are real observations, but they need
  project-specific policy before they can be called true or false boundary
  violations.

## Production-Scope Inference Rerun

The pilot labels drove a tuning pass for inference noise, not a precision
claim. The rerun used the same fixed 200-subject corpus with
`--infer-scope production`. This adds research-oriented excludes for test,
fixture, generated, build, vendored, style, and asset paths; reads package
metadata for public entries; infers consumes from workspace package
dependencies; discovers common app roots such as `app/`, `components/`, and
`lib/`; and avoids treating scoped package namespace directories such as
`packages/@scope` as cells.

Run:

```bash
npm run research:corpus -- \
  --corpus docs/research/corpora/oss-ts-js-200-2026-07-18.json \
  --out reports/corpus/oss-ts-js-200-2026-07-18.production-scope.json \
  --workdir tmp/corpus-precision-study-200-production-final \
  --clone-mode shallow \
  --discard-checkouts \
  --infer-scope production
```

Evidence bundle:

```bash
npm run research:bundle -- \
  --study-id oss-ts-js-200-2026-07-18-production-scope \
  --corpus docs/research/corpora/oss-ts-js-200-2026-07-18.json \
  --report reports/corpus/oss-ts-js-200-2026-07-18.production-scope.json \
  --out-dir reports/corpus/oss-ts-js-200-2026-07-18-production-scope-bundle \
  --force

npm run research:bundle -- \
  --validate \
  --bundle reports/corpus/oss-ts-js-200-2026-07-18-production-scope-bundle
```

Final production-scope summary:

| metric | all-scope infer | production-scope infer | delta |
|---|---:|---:|---:|
| subjects | 200 | 200 | 0 |
| clone/checkout/manifest completed | 200 | 200 | 0 |
| CellFence checks run | 200 | 200 | 0 |
| checks with exit 0 | 14 | 23 | +9 |
| checks with findings | 186 | 177 | -9 |
| configuration/tool/unparseable/timeout failures | 0 | 0 | 0 |
| CLI error findings | 27,817 | 24,417 | -3,400 |
| normalized audit findings, including warnings | 37,654 | 27,616 | -10,038 |
| deterministic labeling sample | 651 | 613 | -38 |

Final production-scope findings by rule:

| rule | all-scope infer | production-scope infer | delta |
|---|---:|---:|---:|
| `CELLFENCE_PRIVATE_IMPORT` | 23,760 | 21,851 | -1,909 |
| `CELLFENCE_UNRESOLVED_IMPORT` | 700 | 1,038 | +338 |
| `CELLFENCE_UNDECLARED_RESOURCE_ACCESS` | 1,722 | 923 | -799 |
| `CELLFENCE_UNRESOLVED_RESOURCE_ACCESS` | 284 | 391 | +107 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_REQUIRE` | 23 | 79 | +56 |
| `CELLFENCE_PUBLIC_ENTRY_MISSING` | 95 | 48 | -47 |
| `CELLFENCE_UNSUPPORTED_DYNAMIC_IMPORT` | 407 | 39 | -368 |
| `CELLFENCE_UNDECLARED_CONSUMER` | 668 | 38 | -630 |
| `CELLFENCE_SYMLINK_TARGET_OUTSIDE_OWNERSHIP` | 1 | 7 | +6 |
| `CELLFENCE_UNOWNED_IMPORT_TARGET` | 138 | 2 | -136 |
| `CELLFENCE_UNOWNED_SOURCE` | 0 | 1 | +1 |
| `CELLFENCE_OWNERSHIP_OVERLAP` | 19 | 0 | -19 |

Subject duration distribution:

| percentile | duration |
|---|---:|
| p50 | 3.6s |
| p90 | 7.0s |
| p99 | 20.2s |

Top production-scope subjects by finding count:

| subject | findings | dominant rules |
|---|---:|---|
| `gitlawb-openclaude` | 9,183 | private imports, unresolved imports, undeclared resources |
| `umami-software-umami` | 2,201 | private imports, undeclared resources |
| `typeorm-typeorm` | 1,675 | private imports, unresolved resources |
| `ajaxorg-ace` | 1,179 | private imports, unresolved imports |
| `maotoumao-musicfree` | 971 | private imports |
| `yeachan-heo-oh-my-codex` | 546 | private imports, undeclared resources, dynamic imports |
| `rollup-rollup` | 497 | private imports, unresolved imports |
| `dokploy-dokploy` | 429 | unresolved resources, private imports, undeclared resources |
| `chatboxai-chatbox` | 425 | private imports, undeclared resources |
| `rohitg00-agentmemory` | 420 | private imports |

Interpretation of the rerun:

- This still is not a CellFence precision number. All subjects still use
  unreviewed inferred manifests.
- The tuning removed the clearest inference bugs: ownership overlap fell to
  zero, asset/style unowned import targets nearly disappeared, dynamic import
  noise from test/example/generated scopes dropped sharply, and inferred
  workspace package dependencies stopped producing most undeclared-consumer
  noise.
- Some unresolved import/resource and dynamic require counts increased because
  repositories that previously fell back to the dead example manifest now have
  real app/package cells and therefore real production files are scanned.
- The remaining `PUBLIC_ENTRY_MISSING` cases are concentrated in interview,
  awesome-list, slide, docs, and education repositories that are weak matches
  for a production source-boundary corpus. They should be filtered or assigned
  a separate corpus stratum before any public precision claim.
- Dynamic import/require and dynamic resource selector findings are the cleanest
  immediate true-positive families, except where they occur in tests, fixtures,
  bundles, examples, or build artifacts.
- The next product work should improve default scope filtering, generated/asset
  target handling, workspace dependency inference, and public-entry inference
  before publishing any precision percentage.
