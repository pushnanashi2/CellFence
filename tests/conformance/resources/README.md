# Resource Conformance

This ledger pins resource-contract behavior for static source analysis and
runtime resource evidence.

The cases cover TypeScript file/HTTP/Prisma observations, Python
FastAPI/SQLAlchemy/Celery observations, unresolved dynamic file selectors, and
runtime evidence JSON.

Run it with:

```sh
npm run conformance:resources
```
