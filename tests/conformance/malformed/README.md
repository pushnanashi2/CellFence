# Malformed Input Conformance

This ledger fixes fail-closed behavior for invalid manifests, baselines, runtime
resource evidence, and waiver comments.

Run it with:

```sh
npm run conformance:malformed
```
