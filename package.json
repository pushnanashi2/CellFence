{
  "name": "cellfence-workspace",
  "version": "0.1.1",
  "private": true,
  "description": "Manifest-driven repository governance for parallel coding agents.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/schema packages/engine packages/cli packages/github-action",
    "clean": "node scripts/clean.mjs",
    "lint": "node scripts/lint.mjs",
    "typecheck": "tsc -b packages/schema packages/engine packages/cli packages/github-action --pretty false",
    "test": "npm run build && node --test tests/*.test.mjs",
    "cellfence:self-check": "npm run build && node packages/cli/dist/index.js baseline check --manifest cellfence.manifest.json --baseline cellfence.baseline.json",
    "pack:smoke": "node scripts/pack-smoke.mjs",
    "provenance:scan": "node scripts/forbidden-source-scan.mjs",
    "release:verify": "node scripts/release-verify.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "typescript": "^5.5.4"
  }
}
