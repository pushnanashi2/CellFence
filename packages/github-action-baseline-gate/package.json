{
  "name": "@cellfence/github-action-baseline-gate",
  "version": "0.0.1",
  "description": "Private bundled GitHub Action that gates CellFence baseline changes on baseline-owner approval.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
