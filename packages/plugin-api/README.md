# @cellfence/plugin-api

Stable TypeScript interfaces for CellFence Plugin API v1.

Use this package to define programmatic CellFence rules, resource adapters, and reporters without depending on engine internals. Adapters translate framework-specific source patterns into common resource access records; CellFence core still performs manifest, baseline, waiver, severity, and resource-contract enforcement.
