# @cellfence/plugin-quants-trend

Rule plugin that warns on abnormal architecture growth momentum.

Historical baselines are evaluated chronologically by `generatedAt`, so callers may pass `history` in repository, API, or filesystem order without changing the result. Baselines with unparseable timestamps are evaluated after parseable entries while preserving their input order.
